I want to develop a system application using the attached template and setup a base, that will help me store and quickly access URL links and folders - called QuickerPlaces.

The UI will only open when the app is open - no minimize to system tray behavior needed. It will need two entry and validation process' for adding folder paths and URL's to the list of remembered 'places', and it should prompt the user for entering an Alias before they enter the list, with checking to ensure the Alias is not in use and the resource is not already stored.

The list of Alias' should be available in a DataGrid below, with the ability to right click them which presents the option to rename the Alias or adjust the link/folder path, as well as remove the keypair if the user wishes. The top option should be Open, and double clicking on the row should similarly shortcut as the Open command.

Users should be able to 'favourite' the main links, which puts them as a bubble in a UI above the data grid, and the data grid can be collapsed if desired. The bubbles simply show the alias' and Open their resource when clicked.

All data for the app should be constantly written to a Json settings dump, as well as being allowed to be exported and imported at will (the user selects any number of stored paths to export or import from what is ingested on import, that is not already in their paths/alias').

Please ask any clarifying queries before we proceed, upon which I will provide the template as well to you. I want our outcome to become a clear SI before we proceed to that step in a second chat for this Project that I will insitagate with the SI and template.