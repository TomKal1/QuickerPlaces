namespace QuickerPlaces;

/// <summary>
/// Single source of truth for the app's display name, header monogram, and
/// AppData publisher folder. The window title, the header glyph, and the
/// settings/places file paths all read from here.
/// </summary>
public static class AppInfo
{
    public const string Name = "QuickerPlaces";
    public const string Monogram = "QP";
    public const string Publisher = "QuickerPlaces";
}
