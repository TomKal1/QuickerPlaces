<Window x:Class="QuickerPlaces.Views.MessageForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height"
        Width="420"
        WindowStartupLocation="CenterOwner"
        d:DesignHeight="160" d:DesignWidth="420">
    <!--
        The app's themed stand-in for MessageBox/TaskDialog — see
        README.md "Dialogs". Being a plain Window, it automatically picks
        up the app-wide Window style (Bg.Glow background, Font.Sans,
        Text.Primary) from Theme.xaml; only its own content below needs
        laying out. Built via MessageForm.Show(...), not instantiated
        directly — see MessageForm.xaml.cs.
    -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--
            A two-column Grid, not a horizontal StackPanel: a StackPanel gives
            the text unlimited width, so it wrapped only at a fixed MaxWidth,
            which with the icon was wider than the window's client area once
            the window border was taken off, clipping the end of each line.
            The star column wraps the text to the room actually left.
        -->
        <Grid Grid.Row="0" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border x:Name="IconBadge" Width="36" Height="36" CornerRadius="18"
                    VerticalAlignment="Top" Margin="0,0,14,0">
                <TextBlock x:Name="IconGlyph" FontWeight="Bold" FontSize="16"
                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock x:Name="MessageText" Grid.Column="1" TextWrapping="Wrap" FontSize="14"
                       VerticalAlignment="Center" />
        </Grid>

        <StackPanel x:Name="ButtonPanel" Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,24,0,0" />
    </Grid>
</Window>
